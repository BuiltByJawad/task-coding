# Backend environment example for MERN Shop

# Port the Express server will listen on
PORT=5000

# MongoDB connection string (replace with your own Atlas URI)
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.xxxxxx.mongodb.net/mernshop

# Secret key used to sign JWT tokens
JWT_SECRET=your_jwt_secret_here

# How long issued JWT tokens remain valid
JWT_EXPIRES_IN=7d
